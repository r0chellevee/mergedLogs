'use strict'

module.exports = (logSources, printer) => {
  //declare storage array 'logs'

  //iterate over logSources
  
  //for each logSource
  
    //push into logs array

  //fast-sort array, call sortedArray

  //print each log of sortedArray

  //call done for stats
}
