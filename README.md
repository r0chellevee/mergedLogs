# mergedLogs

#To recreate the magic:
1. Fork and clone repository or download as .zip file
2. On your CLI, `cd` into local repository
3. Once in directory, run `npm install`
4. Run `npm start` to print logs and stats